name: Check - Devcontainer
on:
  pull_request:
    paths:
      - "**.rs"
      - ".devcontainer/**"
      - ".github/workflows/check-devcontainer.yaml"
      - "Cargo.lock"
      - "Cargo.toml"
      - "rust-toolchain.toml"
      - "src/**/input-*.txt"
  workflow_call:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  wf:
    uses: nikobockerman/github-workflows/.github/workflows/check-devcontainer.yaml@227dcb13eb6b8e20859b6561ddd38632047b12bd
    with:
      runCmd: cargo run --verbose --locked --profile dev -- -vvvv all
